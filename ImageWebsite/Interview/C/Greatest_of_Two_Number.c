#include <stdio.h>

int main() {
    // 1. Declare and assign two numbers
    int num1 = 45;
    int num2 = 78;

    // 2. Use if-else to find the greatest
    if (num1 > num2) {
        printf("%d is the greatest.\n", num1);
    } else {
        printf("%d is the greatest.\n", num2);
    }

    return 0;
}