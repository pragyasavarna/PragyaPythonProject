# --- Core Django ---
Django
python-dotenv
pillow

# --- AI / NLP Models ---
torch
transformers
sentence-transformers
keybert
nltk

# --- System & Utilities (Works on both) ---
psutil
pyttsx3
numpy
pandas
scikit-learn
xgboost
seaborn
kagglehub
pytesseract
tensorflow
django-import-export
nbformat
nbconvert

# --- GeoIP (MaxMind) ---
geoip2

# --- Web Interface ---
gradio

# --- Windows ONLY (Skipped on DigitalOcean) ---
win10toast; sys_platform == 'win32'
pywin32; sys_platform == 'win32'
pyautogui; sys_platform == 'win32'
pyperclip; sys_platform == 'win32'

# --- Linux/Server ONLY (Skipped on Windows) ---
gunicorn; sys_platform != 'win32'
psycopg2-binary; sys_platform != 'win32'